---
import Layout from "@/layouts/Layout.astro";
import RegisterForm from "@/components/auth/RegisterForm";

// Middleware already handles redirect for authenticated users
// This page is only accessible to non-authenticated users
---

<Layout title="Zarejestruj się | 10xCards">
  <main class="container mx-auto py-8 px-4 min-h-screen flex items-center justify-center">
    <RegisterForm client:load />
  </main>
</Layout>
